<svg viewBox="0 0 1000 680" width="1000" height="680" xmlns="http://www.w3.org/2000/svg"><style>@font-face { font-family: 'JetBrains Mono'; font-weight: 400; src: local('JetBrains Mono'), local('JetBrainsMono-Regular'); }
@font-face { font-family: 'JetBrains Mono'; font-weight: 500; src: local('JetBrains Mono'), local('JetBrainsMono-Regular'); }
@font-face { font-family: 'JetBrains Mono'; font-weight: 600; src: local('JetBrains Mono'), local('JetBrainsMono-Regular'); }
@font-face { font-family: 'JetBrains Mono'; font-weight: 700; src: local('JetBrains Mono'), local('JetBrainsMono-Regular'); }
svg { font-family: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace; }

    /* ==========================================================
       THEME VARIABLES — switch by toggling [data-theme] on &lt;html&gt;
       ========================================================== */
    :root,
    [data-theme="dark"] {
      --bg: #020617;
      --grid: #1e293b;
      --text: #ffffff;
      --text-muted: #94a3b8;
      --text-dim: #475569;

      --panel: rgba(15, 23, 42, 0.5);
      --panel-border: #1e293b;
      --lane-fill: rgba(15, 23, 42, 0.22);
      --lane-stroke: #334155;

      --arrow: #64748b;
      --arrow-emphasis: #34d399;

      --mask: #0f172a;

      --frontend-fill:    rgba(8, 51, 68, 0.4);
      --frontend-stroke:  #22d3ee;
      --backend-fill:     rgba(6, 78, 59, 0.4);
      --backend-stroke:   #34d399;
      --database-fill:    rgba(76, 29, 149, 0.4);
      --database-stroke:  #a78bfa;
      --cloud-fill:       rgba(120, 53, 15, 0.3);
      --cloud-stroke:     #fbbf24;
      --security-fill:    rgba(136, 19, 55, 0.4);
      --security-stroke:  #fb7185;
      --messagebus-fill:  rgba(251, 146, 60, 0.3);
      --messagebus-stroke:#fb923c;
      --external-fill:    rgba(30, 41, 59, 0.5);
      --external-stroke:  #94a3b8;

      --toolbar-bg:       rgba(15, 23, 42, 0.8);
      --toolbar-border:   #334155;
      --toolbar-text:     #e2e8f0;
      --toolbar-hover:    rgba(15, 23, 42, 0.95);
      --toolbar-menu-bg:  #0f172a;
    }

    [data-theme="light"] {
      --bg: #f8fafc;
      --grid: #e2e8f0;
      --text: #0f172a;
      --text-muted: #64748b;
      --text-dim: #94a3b8;

      --panel: #ffffff;
      --panel-border: #e2e8f0;
      --lane-fill: rgba(248, 250, 252, 0.65);
      --lane-stroke: #cbd5e1;

      --arrow: #94a3b8;
      --arrow-emphasis: #059669;

      --mask: #ffffff;

      --frontend-fill:    rgba(34, 211, 238, 0.15);
      --frontend-stroke:  #0891b2;
      --backend-fill:     rgba(52, 211, 153, 0.18);
      --backend-stroke:   #059669;
      --database-fill:    rgba(167, 139, 250, 0.2);
      --database-stroke:  #7c3aed;
      --cloud-fill:       rgba(251, 191, 36, 0.18);
      --cloud-stroke:     #d97706;
      --security-fill:    rgba(251, 113, 133, 0.15);
      --security-stroke:  #e11d48;
      --messagebus-fill:  rgba(251, 146, 60, 0.15);
      --messagebus-stroke:#ea580c;
      --external-fill:    rgba(148, 163, 184, 0.18);
      --external-stroke:  #64748b;

      --toolbar-bg:       rgba(255, 255, 255, 0.92);
      --toolbar-border:   #cbd5e1;
      --toolbar-text:     #334155;
      --toolbar-hover:    #ffffff;
      --toolbar-menu-bg:  #ffffff;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace;
      background: var(--bg);
      min-height: 100vh;
      padding: 2rem;
      color: var(--text);
      transition: background 0.2s ease, color 0.2s ease;
    }

    .container { max-width: 1200px; margin: 0 auto; }

    .header { margin-bottom: 2rem; }

    .header-row {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 0.5rem;
    }

    .pulse-dot {
      width: 12px;
      height: 12px;
      background: var(--frontend-stroke);
      border-radius: 50%;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    h1 {
      font-size: 1.5rem;
      font-weight: 700;
      letter-spacing: -0.025em;
    }

    .subtitle {
      color: var(--text-muted);
      font-size: 0.875rem;
      margin-left: 1.75rem;
    }

    .diagram-container {
      background: var(--panel);
      border-radius: 1rem;
      border: 1px solid var(--panel-border);
      padding: 1.5rem;
      overflow-x: auto;
    }

    svg {
      width: 100%;
      min-width: min(900px, 100%);
      display: block;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1rem;
      margin-top: 2rem;
    }

    .card {
      background: var(--panel);
      border-radius: 0.75rem;
      border: 1px solid var(--panel-border);
      padding: 1.25rem;
    }

    .card-header {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.75rem;
    }

    .card-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
    }

    .card-dot.cyan     { background: var(--frontend-stroke); }
    .card-dot.emerald  { background: var(--backend-stroke); }
    .card-dot.violet   { background: var(--database-stroke); }
    .card-dot.amber    { background: var(--cloud-stroke); }
    .card-dot.rose     { background: var(--security-stroke); }
    .card-dot.orange   { background: var(--messagebus-stroke); }
    .card-dot.slate    { background: var(--external-stroke); }

    .card h3 { font-size: 0.875rem; font-weight: 600; color: var(--text); }

    .card ul {
      list-style: none;
      color: var(--text-muted);
      font-size: 0.75rem;
    }

    .card li { margin-bottom: 0.375rem; }

    .footer {
      text-align: center;
      margin-top: 1.5rem;
      color: var(--text-dim);
      font-size: 0.75rem;
    }

    @page { size: landscape; margin: 1.5cm; }

    @media print {
      :root, [data-theme="dark"], [data-theme="light"] {
        --bg: #ffffff;
        --grid: transparent;
        --panel: #ffffff;
        --panel-border: #e2e8f0;
        --text: #0f172a;
        --text-muted: #475569;
        --text-dim: #94a3b8;
        --mask: #ffffff;
      }
      body { background: #ffffff !important; padding: 0; }
      .container { max-width: none; }
      .toolbar, .archify-toast { display: none !important; }
      .diagram-container, .card { box-shadow: none; border-color: #e2e8f0; }
      .cards { grid-template-columns: 1fr 1fr; }
      .card { break-inside: avoid; page-break-inside: avoid; }
      .diagram-container { break-inside: avoid; page-break-inside: avoid; }
      h1, .subtitle { color: #0f172a; }
    }

    /* ==========================================================
       TOOLBAR (theme toggle + export menu)
       ========================================================== */
    .toolbar {
      position: fixed;
      top: 1rem;
      right: 1rem;
      display: flex;
      gap: 0.5rem;
      z-index: 100;
    }
    .toolbar button {
      background: var(--toolbar-bg);
      color: var(--toolbar-text);
      border: 1px solid var(--toolbar-border);
      padding: 0.5rem 0.875rem;
      border-radius: 0.5rem;
      font-family: inherit;
      font-size: 0.75rem;
      font-weight: 500;
      cursor: pointer;
      backdrop-filter: blur(8px);
      transition: background 0.15s, border-color 0.15s;
      display: inline-flex;
      align-items: center;
      gap: 0.375rem;
    }
    .toolbar button:hover {
      background: var(--toolbar-hover);
      border-color: var(--arrow);
    }
    .toolbar button:focus-visible {
      outline: 2px solid var(--frontend-stroke);
      outline-offset: 2px;
    }
    .toolbar .export-wrap { position: relative; }
    .toolbar .export-menu {
      position: absolute;
      top: calc(100% + 0.375rem);
      right: 0;
      background: var(--toolbar-menu-bg);
      border: 1px solid var(--toolbar-border);
      border-radius: 0.5rem;
      padding: 0.375rem;
      min-width: 160px;
      display: none;
      flex-direction: column;
      box-shadow: 0 4px 20px rgba(0,0,0,0.25);
    }
    .toolbar .export-menu.open { display: flex; }
    .toolbar .export-menu button {
      background: transparent;
      border: 0;
      justify-content: space-between;
      width: 100%;
      text-align: left;
      padding: 0.5rem 0.75rem;
    }
    .toolbar .export-menu button:hover {
      background: var(--toolbar-hover);
    }
    .toolbar .export-menu .hint {
      color: var(--text-dim);
      font-size: 0.625rem;
    }
    .toolbar .export-menu hr {
      border: 0;
      border-top: 1px solid var(--toolbar-border);
      margin: 0.25rem 0.375rem;
      opacity: 0.5;
    }

    .archify-toast {
      position: fixed;
      top: 1rem;
      left: 50%;
      transform: translateX(-50%) translateY(-8px);
      background: var(--toolbar-menu-bg);
      color: var(--text);
      border: 1px solid var(--toolbar-border);
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      font-family: inherit;
      font-size: 0.75rem;
      opacity: 0;
      transition: opacity 0.2s, transform 0.2s;
      z-index: 200;
      pointer-events: none;
      box-shadow: 0 4px 20px rgba(0,0,0,0.25);
    }
    .archify-toast.show {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }

    @media (max-width: 720px) {
      body { padding: 1rem; }
      .toolbar {
        position: static;
        justify-content: flex-end;
        margin-bottom: 1rem;
      }
      .header { margin-bottom: 1.25rem; }
      .header-row {
        gap: 0.75rem;
        align-items: flex-start;
      }
      h1 {
        font-size: 1.25rem;
        line-height: 1.25;
      }
      .subtitle {
        margin-left: 0;
        font-size: 0.8125rem;
        line-height: 1.55;
      }
      .diagram-container {
        padding: 0.75rem;
        border-radius: 0.75rem;
      }
      .cards {
        grid-template-columns: 1fr;
        margin-top: 1rem;
      }
    }

    /* ==========================================================
       SVG SEMANTIC CLASSES
       ========================================================== */
    .c-grid       { stroke: var(--grid); fill: none; }
    .c-mask       { fill: var(--mask); stroke: none; }

    .c-frontend   { fill: var(--frontend-fill);   stroke: var(--frontend-stroke); }
    .c-backend    { fill: var(--backend-fill);    stroke: var(--backend-stroke); }
    .c-database   { fill: var(--database-fill);   stroke: var(--database-stroke); }
    .c-cloud      { fill: var(--cloud-fill);      stroke: var(--cloud-stroke); }
    .c-security   { fill: var(--security-fill);   stroke: var(--security-stroke); }
    .c-messagebus { fill: var(--messagebus-fill); stroke: var(--messagebus-stroke); }
    .c-external   { fill: var(--external-fill);   stroke: var(--external-stroke); }

    .t-primary { fill: var(--text); }
    .t-muted   { fill: var(--text-muted); }
    .t-dim     { fill: var(--text-dim); }
    .t-frontend   { fill: var(--frontend-stroke); }
    .t-backend    { fill: var(--backend-stroke); }
    .t-database   { fill: var(--database-stroke); }
    .t-cloud      { fill: var(--cloud-stroke); }
    .t-security   { fill: var(--security-stroke); }
    .t-messagebus { fill: var(--messagebus-stroke); }
    .t-external   { fill: var(--external-stroke); }

    .a-default   { stroke: var(--arrow); fill: none; }
    .a-emphasis  { stroke: var(--arrow-emphasis); fill: none; }
    .a-security  { stroke: var(--security-stroke); fill: none; stroke-dasharray: 5,5; }
    .a-dashed    { stroke: var(--database-stroke); fill: none; stroke-dasharray: 4,4; }

    .m-default   { fill: var(--arrow); }
    .m-emphasis  { fill: var(--arrow-emphasis); }
    .m-security  { fill: var(--security-stroke); }
    .m-dashed    { fill: var(--database-stroke); }

    .c-security-group { fill: transparent; stroke: var(--security-stroke); stroke-dasharray: 4,4; }
    .c-region         { fill: rgba(251, 191, 36, 0.05); stroke: var(--cloud-stroke); stroke-dasharray: 8,4; }
    .c-lane           { fill: var(--lane-fill); stroke: var(--lane-stroke); stroke-dasharray: 6,6; }
  
:root, svg { --bg: #020617; --grid: #1e293b; --text: #ffffff; --text-muted: #94a3b8; --text-dim: #475569; --arrow: #64748b; --arrow-emphasis: #34d399; --mask: #0f172a; --frontend-fill: rgba(8, 51, 68, 0.4); --frontend-stroke: #22d3ee; --backend-fill: rgba(6, 78, 59, 0.4); --backend-stroke: #34d399; --database-fill: rgba(76, 29, 149, 0.4); --database-stroke: #a78bfa; --cloud-fill: rgba(120, 53, 15, 0.3); --cloud-stroke: #fbbf24; --security-fill: rgba(136, 19, 55, 0.4); --security-stroke: #fb7185; --messagebus-fill: rgba(251, 146, 60, 0.3); --messagebus-stroke: #fb923c; --external-fill: rgba(30, 41, 59, 0.5); --external-stroke: #94a3b8; }
@media (prefers-color-scheme: light) { :root, svg { --bg: #f8fafc; --grid: #e2e8f0; --text: #0f172a; --text-muted: #64748b; --text-dim: #94a3b8; --arrow: #94a3b8; --arrow-emphasis: #059669; --mask: #ffffff; --frontend-fill: rgba(34, 211, 238, 0.15); --frontend-stroke: #0891b2; --backend-fill: rgba(52, 211, 153, 0.18); --backend-stroke: #059669; --database-fill: rgba(167, 139, 250, 0.2); --database-stroke: #7c3aed; --cloud-fill: rgba(251, 191, 36, 0.18); --cloud-stroke: #d97706; --security-fill: rgba(251, 113, 133, 0.15); --security-stroke: #e11d48; --messagebus-fill: rgba(251, 146, 60, 0.15); --messagebus-stroke: #ea580c; --external-fill: rgba(148, 163, 184, 0.18); --external-stroke: #64748b; } }
svg[data-theme="light"] { --bg: #f8fafc; --grid: #e2e8f0; --text: #0f172a; --text-muted: #64748b; --text-dim: #94a3b8; --arrow: #94a3b8; --arrow-emphasis: #059669; --mask: #ffffff; --frontend-fill: rgba(34, 211, 238, 0.15); --frontend-stroke: #0891b2; --backend-fill: rgba(52, 211, 153, 0.18); --backend-stroke: #059669; --database-fill: rgba(167, 139, 250, 0.2); --database-stroke: #7c3aed; --cloud-fill: rgba(251, 191, 36, 0.18); --cloud-stroke: #d97706; --security-fill: rgba(251, 113, 133, 0.15); --security-stroke: #e11d48; --messagebus-fill: rgba(251, 146, 60, 0.15); --messagebus-stroke: #ea580c; --external-fill: rgba(148, 163, 184, 0.18); --external-stroke: #64748b; }
svg[data-theme="dark"] { --bg: #020617; --grid: #1e293b; --text: #ffffff; --text-muted: #94a3b8; --text-dim: #475569; --arrow: #64748b; --arrow-emphasis: #34d399; --mask: #0f172a; --frontend-fill: rgba(8, 51, 68, 0.4); --frontend-stroke: #22d3ee; --backend-fill: rgba(6, 78, 59, 0.4); --backend-stroke: #34d399; --database-fill: rgba(76, 29, 149, 0.4); --database-stroke: #a78bfa; --cloud-fill: rgba(120, 53, 15, 0.3); --cloud-stroke: #fbbf24; --security-fill: rgba(136, 19, 55, 0.4); --security-stroke: #fb7185; --messagebus-fill: rgba(251, 146, 60, 0.3); --messagebus-stroke: #fb923c; --external-fill: rgba(30, 41, 59, 0.5); --external-stroke: #94a3b8; }
rect.c-bg-rect { fill: var(--bg); }
</style><rect width="100%" height="100%" class="c-bg-rect"/>
        <defs>
          <marker id="arrowhead" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-default"/>
          </marker>
          <marker id="arrowhead-emphasis" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-emphasis"/>
          </marker>
          <marker id="arrowhead-security" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-security"/>
          </marker>
          <marker id="arrowhead-dashed" markerWidth="10" markerHeight="7" refX="9" refY="3.5" orient="auto">
            <polygon points="0 0, 10 3.5, 0 7" class="m-dashed"/>
          </marker>
          <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" class="c-grid" stroke-width="0.5"/>
          </pattern>
        </defs>

        <rect width="100%" height="100%" fill="url(#grid)"/>

        <!-- ================================================================
             ARROWS (drawn first so component boxes mask them)
             ================================================================ -->

        <!-- 调用方 -> SnapshotRenderer -->
        <line x1="150" y1="310" x2="198" y2="310" class="a-default" stroke-width="1.5" marker-end="url(#arrowhead)"/>
        <text x="174" y="303" class="t-muted" font-size="8" text-anchor="middle">await</text>

        <!-- .vue 文件 -> vue3tohtml -->
        <line x1="320" y1="140" x2="388" y2="140" class="a-default" stroke-width="1.5" marker-end="url(#arrowhead)"/>

        <!-- vue3tohtml -> Chromium（内存 HTML 字符串） -->
        <path d="M 465 180 L 465 220 L 640 220 L 640 258" class="a-emphasis" stroke-width="1.5" marker-end="url(#arrowhead-emphasis)"/>
        <text x="553" y="213" class="t-muted" font-size="8" text-anchor="middle">HTML 字符串（内存）</text>

        <!-- SnapshotRenderer -> Chromium（控制流：启动浏览器 / 建 page） -->
        <line x1="360" y1="345" x2="558" y2="345" class="a-dashed" stroke-width="1.5" marker-end="url(#arrowhead-dashed)"/>
        <text x="459" y="338" class="t-muted" font-size="8" text-anchor="middle">launch · newPage</text>

        <!-- 网址 -> Chromium（goto 直达） -->
        <path d="M 320 500 L 480 500 L 480 375 L 558 375" class="a-default" stroke-width="1.5" marker-end="url(#arrowhead)"/>
        <text x="427" y="493" class="t-muted" font-size="8" text-anchor="middle">goto · networkidle</text>

        <!-- fontFamily 注入 -> Chromium -->
        <line x1="640" y1="470" x2="640" y2="372" class="a-default" stroke-width="1.5" marker-end="url(#arrowhead)"/>

        <!-- Chromium -> 输出 -->
        <line x1="720" y1="315" x2="858" y2="315" class="a-emphasis" stroke-width="1.5" marker-end="url(#arrowhead-emphasis)"/>
        <text x="789" y="308" class="t-muted" font-size="8" text-anchor="middle">screenshot</text>

        <!-- ================================================================
             BOUNDARY
             ================================================================ -->
        <rect x="170" y="60" width="660" height="560" rx="12" class="c-region" stroke-width="1"/>
        <text x="182" y="78" class="t-cloud" font-size="10" font-weight="600">vue3renderer</text>

        <!-- ================================================================
             COMPONENTS
             ================================================================ -->

        <!-- 调用方 -->
        <rect x="30" y="280" width="120" height="60" rx="6" class="c-mask"/>
        <rect x="30" y="280" width="120" height="60" rx="6" class="c-external" stroke-width="1.5"/>
        <text x="90" y="304" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">调用方</text>
        <text x="90" y="322" class="t-muted" font-size="9" text-anchor="middle">Node.js ESM</text>

        <!-- .vue 文件 + props -->
        <rect x="200" y="110" width="120" height="60" rx="6" class="c-mask"/>
        <rect x="200" y="110" width="120" height="60" rx="6" class="c-external" stroke-width="1.5"/>
        <text x="260" y="134" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">.vue 文件</text>
        <text x="260" y="152" class="t-muted" font-size="9" text-anchor="middle">+ props JSON</text>

        <!-- vue3tohtml -->
        <rect x="390" y="110" width="150" height="70" rx="6" class="c-mask"/>
        <rect x="390" y="110" width="150" height="70" rx="6" class="c-database" stroke-width="1.5"/>
        <text x="465" y="132" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">vue3tohtml</text>
        <text x="465" y="150" class="t-muted" font-size="8" text-anchor="middle">SSR 编译 · 内存 HTML</text>
        <text x="465" y="164" class="t-muted" font-size="8" text-anchor="middle">props 透传 defineProps</text>

        <!-- SnapshotRenderer 核心 -->
        <rect x="200" y="280" width="160" height="110" rx="6" class="c-mask"/>
        <rect x="200" y="280" width="160" height="110" rx="6" class="c-backend" stroke-width="1.5"/>
        <text x="280" y="302" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">SnapshotRenderer</text>
        <text x="280" y="322" class="t-muted" font-size="8" text-anchor="middle">• 信号量限流 · FIFO 排队</text>
        <text x="280" y="336" class="t-muted" font-size="8" text-anchor="middle">• 浏览器懒启动 · 崩溃自愈</text>
        <text x="280" y="350" class="t-muted" font-size="8" text-anchor="middle">• 默认值合并 / 单次覆盖</text>
        <text x="280" y="372" class="t-backend" font-size="7" text-anchor="middle">createSnapshotter(options)</text>

        <!-- Chromium -->
        <rect x="560" y="260" width="160" height="110" rx="6" class="c-mask"/>
        <rect x="560" y="260" width="160" height="110" rx="6" class="c-frontend" stroke-width="1.5"/>
        <text x="640" y="282" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">Chromium 常驻</text>
        <text x="640" y="302" class="t-muted" font-size="8" text-anchor="middle">• Playwright 驱动</text>
        <text x="640" y="316" class="t-muted" font-size="8" text-anchor="middle">• 每截图独立 page/context</text>
        <text x="640" y="330" class="t-muted" font-size="8" text-anchor="middle">• 视口 / 缩放 / 深色 / 时区</text>
        <text x="640" y="352" class="t-frontend" font-size="7" text-anchor="middle">内核：executablePath / CHROME_PATH / 探测</text>

        <!-- 网址输入 -->
        <rect x="200" y="470" width="120" height="60" rx="6" class="c-mask"/>
        <rect x="200" y="470" width="120" height="60" rx="6" class="c-external" stroke-width="1.5"/>
        <text x="260" y="494" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">网址</text>
        <text x="260" y="512" class="t-muted" font-size="9" text-anchor="middle">http(s)://</text>

        <!-- fontFamily 注入 -->
        <rect x="560" y="470" width="160" height="60" rx="6" class="c-mask"/>
        <rect x="560" y="470" width="160" height="60" rx="6" class="c-cloud" stroke-width="1.5"/>
        <text x="640" y="494" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">fontFamily 注入</text>
        <text x="640" y="512" class="t-muted" font-size="8" text-anchor="middle">缺失回退兜底 · document.fonts 等待</text>

        <!-- 输出 -->
        <rect x="860" y="285" width="110" height="60" rx="6" class="c-mask"/>
        <rect x="860" y="285" width="110" height="60" rx="6" class="c-database" stroke-width="1.5"/>
        <text x="915" y="309" class="t-primary" font-size="11" font-weight="600" text-anchor="middle">PNG / JPEG</text>
        <text x="915" y="327" class="t-muted" font-size="8" text-anchor="middle">Buffer → 文件 / HTTP</text>

        <!-- ================================================================
             LEGEND
             ================================================================ -->
        <text x="30" y="424" class="t-primary" font-size="10" font-weight="600">Legend</text>

        <rect x="30" y="436" width="16" height="10" rx="2" class="c-backend" stroke-width="1"/>
        <text x="52" y="444" class="t-muted" font-size="8">核心逻辑</text>

        <rect x="30" y="454" width="16" height="10" rx="2" class="c-frontend" stroke-width="1"/>
        <text x="52" y="462" class="t-muted" font-size="8">浏览器渲染</text>

        <rect x="30" y="472" width="16" height="10" rx="2" class="c-database" stroke-width="1"/>
        <text x="52" y="480" class="t-muted" font-size="8">编译产物 / 输出</text>

        <rect x="30" y="490" width="16" height="10" rx="2" class="c-external" stroke-width="1"/>
        <text x="52" y="498" class="t-muted" font-size="8">输入</text>

        <rect x="30" y="508" width="16" height="10" rx="2" class="c-cloud" stroke-width="1"/>
        <text x="52" y="516" class="t-muted" font-size="8">字体</text>

        <line x1="30" y1="532" x2="46" y2="532" class="a-emphasis" stroke-width="1.5"/>
        <text x="52" y="535" class="t-muted" font-size="8">主数据流</text>

        <line x1="30" y1="548" x2="46" y2="548" class="a-dashed" stroke-width="1.5"/>
        <text x="52" y="551" class="t-muted" font-size="8">控制流</text>
      </svg>